A variable with "any" type will have it's real type evaluated at runtime. When assigning a new value to a variable with the [[Any]] type, the [[Type]] will change to the [[Type]] of the new value.

> [!Warning]
> It is highly advised to type out full types for variables, functions and parameters. Use [[Any]] only as a last resort!